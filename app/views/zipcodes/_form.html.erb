<%= form_for(@zipcode) do |f| %>
  <% if @zipcode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zipcode.errors.count, "error") %> prohibited this zipcode from being saved:</h2>

      <ul>
      <% @zipcode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :provincia %><br />
    <%= f.text_field :provincia %>
  </div>
  <div class="field">
    <%= f.label :sector %><br />
    <%= f.text_area :sector %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
